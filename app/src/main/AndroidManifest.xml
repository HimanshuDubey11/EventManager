<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.himanshu.com.eventmanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.StudentSignUp" />
        <activity
            android:name=".activity.StudentHome"
            android:label="@string/title_activity_student_home" />
        <activity
            android:name=".activity.AdminHome"
            android:label="@string/title_activity_admin_home" />
        <activity android:name=".activity.AdminSignUp" />
        <activity android:name=".utility.EventAdapter" />
        <activity android:name=".activity.StudentEventActivity" />
        <activity android:name=".PayUPaymentActivity"/>
        <activity android:name=".activity.StudentRegisteredActivity"></activity>
    </application>

</manifest>